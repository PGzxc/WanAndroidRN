import { resolvedTokenization } from '../utils';
export function StyledValueToCSSObject(input, CONFIG) {
  if (!input) {
    return {};
  }
  return resolvedTokenization(input, CONFIG);
}
//# sourceMappingURL=StyledValueToCSSObject.js.map