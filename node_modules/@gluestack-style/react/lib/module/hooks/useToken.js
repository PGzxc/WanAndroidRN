import { useStyled } from '../StyledProvider';
/**
 *
 * @param tokenScale Type of the token ex: colors, spacing, fontSizes, etc
 * @param token Token name ex: red500, 1, sm, etc
 * @returns
 */
export const useToken = (tokenScale, token) => {
  var _themeTokens;
  const theme = useStyled();
  // @ts-ignore
  const themeTokens = theme.config.tokens;
  return themeTokens === null || themeTokens === void 0 || (_themeTokens = themeTokens[`${tokenScale}`]) === null || _themeTokens === void 0 ? void 0 : _themeTokens[`${token}`];
};
//# sourceMappingURL=useToken.js.map